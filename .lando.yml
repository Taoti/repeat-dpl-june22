name: drupal8

config:
#   site: pantheon site name
#   id: pantheon site id
  composer_version: '2-latest'

tooling:
  npm:
    service: appserver
    cmd: npm
    dir: /app/web/themes/repeat_dpl_june22
  gulp:
    service: appserver
    cmd: gulp
    dir: /app/web/themes/repeat_dpl_june22
